{
  "name": "renovatie-app",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Verbouwingsplanning uit je eigen Google Sheets. Draait volledig in de browser.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/google.accounts": "^0.0.15",
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "vitest": "^3.0.5"
  }
}
